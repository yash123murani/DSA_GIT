#include<iostream>
using namespace std;


int main(){
    // int a  = 2;
    // int b = 3;

    // int c = (a|b);
    // int d = (a|b);
    // int p = ~a;
    // // std::cout << (~a) << std::endl;
    // std::cout << c << std::endl;
    // std::cout << d << std::endl;
    // std::cout << p << std::endl;
    // cout<<(a^a);    

    // Left swift operaton
    // int a = 2;
    // cout<<(a<<1)<<endl; //swift by the one bit it self in the binary of the thats something to the 32 bit of the combination of the zeros and the onces
    // cout<<(a>>1); //swift the one bit right to the one but of it by the one but of binary of the 2 something around to the 32bits


    // Pre increment and the post incremnt

    int n  = 5;
    cout<<(++n)<<endl; //first this will increment by one then only the print it

    cout<<n++<<endl; //first this will print value of a then increment by one for the future use
    cout<<n<<endl;
    //this all also applied for the decrement operator

    


}